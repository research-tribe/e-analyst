name: Deploy to Ubuntu VM

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install and build
        run: |
          npm ci
          npm run build

      - name: Deploy to VM via SSH
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ vars.AZURE_TEST_VM_HOST }}
          username: ${{ vars.AZURE_TEST_VM_USERNAME }}
          key: ${{ secrets.AZURE_TEST_VM_KEY }}
          port: ${{ secrets.AZURE_TEST_VM_PORT || 22 }}
          source: "dist/*"
          target: "/var/www/tamil-nadu-economic-monitor"
          strip_components: 1

      - name: Reload nginx (optional)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ vars.AZURE_TEST_VM_HOST }}
          username: ${{ vars.AZURE_TEST_VM_USERNAME }}
          key: ${{ secrets.AZURE_TEST_VM_KEY }}
          port: ${{ secrets.AZURE_TEST_VM_PORT || 22 }}
          script: sudo systemctl reload nginx